# Day 10 — Python + SQL Together

## Objective
Drive a database from Python code — your first true backend program.

## Concepts
The sqlite3 module; parameterised queries (and why string-building is dangerous); CRUD functions; structuring a small data layer.

## Watch before the session
- Corey Schafer — sqlite3 episode
- Tech With Tim — Python + SQLite videos
- "SQL Injection in 100 Seconds" — Fireship (why parameters matter)

## Task of the day
Rebuild the bank or library app with SQLite storage instead of JSON — full CRUD from a terminal menu, using parameterised queries only.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
