# Day 7 — Modules, Packages, pip & the Standard Library

## Objective
Organise code across files and stand on the shoulders of the standard library.

## Concepts
import and modules; packages; virtual environments; pip and requirements.txt; datetime, os, pathlib, random, json — a guided tour.

## Watch before the session
- Corey Schafer — modules and pip/virtualenv episodes
- "Python Virtual Environments" — Tech With Tim
- "Python datetime" and standard library videos — Corey Schafer

## Task of the day
Split the bank app into modules (models, storage, main), add timestamps to transactions with datetime, and produce a requirements.txt inside a virtual environment.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
