# Day 20 — DRF II — ViewSets, Routers & Full CRUD

## Objective
Write less code and get complete, consistent CRUD — the production pattern.

## Concepts
Generic views; ViewSets and routers; validation in serializers; correct status codes; nested serializers.

## Watch before the session
- Dennis Ivy — DRF series, viewsets/routers episodes
- BugBytes — generic views and viewsets videos
- DRF docs — "Quickstart" (follow once, end to end)

## Task of the day
Refactor everything to ViewSets + a router; add full CRUD for every model. Postman collection updated and pushed with the repo.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
