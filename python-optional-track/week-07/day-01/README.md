# Day 25 — PostgreSQL & Environment Configuration

## Objective
Graduate from SQLite to the database real deployments use.

## Concepts
Installing/using PostgreSQL (local or free cloud tier); psycopg; DATABASES settings; environment variables with django-environ or python-dotenv; separating dev and production settings.

## Watch before the session
- Dennis Ivy — Django + PostgreSQL videos
- "PostgreSQL in 100 Seconds" — Fireship
- Django docs — databases settings page

## Task of the day
Switch your project to PostgreSQL with credentials in a .env file (which is git-ignored!). All tests still pass.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
