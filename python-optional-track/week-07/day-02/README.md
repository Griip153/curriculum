# Day 26 — CORS & Serving a Frontend

## Objective
Let browsers on other domains talk to your API — and see the full picture end to end.

## Concepts
What CORS is; django-cors-headers; a minimal JS/React page consuming your API with the JWT attached; thinking in "backend serves many clients".

## Watch before the session
- "CORS in 100 Seconds" — Fireship
- Dennis Ivy — Django & React integration videos
- django-cors-headers README (short read)

## Task of the day
Build one simple HTML/JS page that logs in against your API, stores the token, and lists + creates records. Watch the network tab while it happens.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
