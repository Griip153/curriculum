# Day 27 — Deployment on Render (heavy day)

## Objective
Put your API on the internet properly — secrets safe, debug off, database connected.

## Concepts
gunicorn; requirements and build commands; environment variables on Render; DEBUG=False, ALLOWED_HOSTS, collectstatic; connecting a hosted PostgreSQL; reading deploy logs.

## Watch before the session
- Dennis Ivy — Django deployment on Render videos
- Render docs — "Deploy a Django app"
- Corey Schafer — deployment episode (concepts)

## Task of the day
Deploy to Render with hosted PostgreSQL. The live URL must respond in everyone’s browser, and your repo must contain zero secrets.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
