# Day 28 — Performance, Security & API Documentation

## Objective
The habits that separate juniors from professionals.

## Concepts
The N+1 problem, select_related/prefetch_related; the Django security checklist; throttling (concept); auto-generated docs with drf-spectacular (Swagger UI); logging basics.

## Watch before the session
- BugBytes — select_related/prefetch_related and drf-spectacular videos
- Django docs — "Security in Django" (read it once fully)
- ArjanCodes — API design quality videos

## Task of the day
Add Swagger docs to your API with drf-spectacular, fix at least one N+1 query (show before/after query counts), and run through the security checklist.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
