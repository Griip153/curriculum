# Day 7 — JavaScript Basics II — Functions, Arrays & Objects

## Objective
Organise code into reusable functions and model real-world data with arrays and objects.

## Concepts
Function declarations and parameters; return values; arrays and key methods (push, map, filter, find); objects and nested data; combining them (arrays of objects).

## Watch before the session
- Continue freeCodeCamp’s JavaScript full course — functions, arrays, objects sections
- "JavaScript Arrays" and "JavaScript Objects" — Bro Code
- "8 Must Know JavaScript Array Methods" — Web Dev Simplified

## Task of the day
Build a "student records" program: an array of student objects, functions to add a student, compute the class average, and list everyone above the average.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
