# Day 10 — Asynchronous JavaScript & Fetch (heavy day)

## Objective
Talk to servers — the skill that turns a web page into a web application.

## Concepts
Callbacks vs promises vs async/await; the event loop (conceptually); fetch(); working with JSON; handling errors and loading states.

## Watch before the session
- "Async JavaScript Crash Course" — Traversy Media
- "What the heck is the event loop anyway?" — Philip Roberts (JSConf talk)
- "Fetch API in 6 Minutes" — Web Dev Simplified

## Task of the day
Build a "random user directory": fetch users from the free randomuser.me API and render them as cards, with a loading message and error handling.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
