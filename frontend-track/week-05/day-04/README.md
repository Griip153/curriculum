# Day 20 — Data Fetching, Loading & Error States

## Objective
Fetch data the Next.js way — with caching, streaming, and graceful failure built in.

## Concepts
async server components; fetch and caching/revalidation basics; loading.js and Suspense; error.js; not-found.

## Watch before the session
- Official docs — "Data Fetching" section
- Net Ninja — data fetching episodes of the App Router playlist
- Dave Gray — Next.js data fetching videos

## Task of the day
Build a "countries explorer" using the free REST Countries API: list page with loading skeleton, plus a proper error state (test it by breaking the URL).

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
