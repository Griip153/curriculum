# Day 22 — Route Handlers — Your Own API

## Objective
Discover that Next.js is full-stack: build API endpoints inside your frontend project.

## Concepts
app/api route handlers; GET/POST/PUT/DELETE; reading request bodies and params; returning JSON with proper status codes; when route handlers vs external APIs.

## Watch before the session
- Official docs — "Route Handlers"
- Dave Gray — route handlers video
- Net Ninja — API routes episodes

## Task of the day
Build a /api/guestbook endpoint (GET + POST, in-memory array) and a page that reads and posts to it.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
