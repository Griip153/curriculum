# Day 23 — Forms & Server Actions (heavy day)

## Objective
Handle user input the modern Next.js way — mutations without writing API plumbing.

## Concepts
Server actions ("use server"); form actions; useFormStatus for pending states; validation and error feedback; revalidatePath.

## Watch before the session
- Official docs — "Server Actions and Mutations"
- Lee Robinson — forms and server actions demos
- Josh tried coding — server actions tutorials

## Task of the day
Rebuild the guestbook with a server action instead of the API route — including a pending state and validation errors shown to the user.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
