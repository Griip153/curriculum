# Day 4 — Node.js & Your First Server

## Objective
Run JavaScript on the server and answer your first HTTP request.

## Concepts
npm and package.json; modules; fs and path; a raw HTTP server with JSON routes; nodemon; .gitignore for node_modules.

## Watch before the session
- "Node.js Crash Course" — Traversy Media
- Net Ninja — Node.js Crash Course playlist
- "Node.js in 100 Seconds" — Fireship

## Task of the day
Build a raw HTTP server with two JSON routes and an npm dev script running it with nodemon. Push with a proper .gitignore.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
