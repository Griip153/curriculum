# Day 2 — Functions, Arrays & Objects

## Objective
Organise code and model real-world data — the shape of everything an API sends and receives.

## Concepts
Functions and parameters; arrays and key methods (map, filter, find); objects; arrays of objects; JSON.

## Watch before the session
- freeCodeCamp JS course — functions, arrays, objects sections
- "8 Must Know JavaScript Array Methods" — Web Dev Simplified
- "JavaScript Objects" — Bro Code

## Task of the day
Build a "student records" terminal program: array of objects, functions for average, top student, and filtering — saved to and loaded from a JSON file.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
