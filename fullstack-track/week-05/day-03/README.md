# Day 19 — REST API Design & Project Structure

## Objective
Organise an API the way professional teams do — so it survives growing.

## Concepts
What REST really means (resources, nouns not verbs); routes vs controllers separation; error-handling middleware; environment variables with dotenv; HTTP status codes done properly.

## Watch before the session
- "REST API Crash Course" — Traversy Media
- Web Dev Simplified — Express project structure videos
- "MVC in 100 seconds" — Fireship (pattern concept)

## Task of the day
Refactor the students API into routes/ and controllers/ folders, add a central error handler and a 404 handler, and move the port into a .env file.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
