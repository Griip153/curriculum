# Day 17 — Node.js — JavaScript Leaves the Browser

## Objective
Run the language you already know on the server — no new language, new superpowers.

## Concepts
What Node.js is; npm and package.json; CommonJS vs ES modules; the fs and path modules; building a raw HTTP server (once, to appreciate Express).

## Watch before the session
- "Node.js Crash Course" — Traversy Media
- Net Ninja — "Node.js Crash Course" playlist (first episodes)
- "Node.js in 100 Seconds" — Fireship

## Task of the day
Initialise an npm project, write a script that reads/writes JSON files, then build a raw HTTP server that returns JSON on two routes. Push to GitHub.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
