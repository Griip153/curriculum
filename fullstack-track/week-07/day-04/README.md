# Day 28 — Deployment on Render & Connecting a Frontend

## Objective
Ship your API to the internet and see the whole system end to end.

## Concepts
Deploying Express on Render; environment variables in production; Atlas network access; connecting a React (or plain JS) frontend with the JWT attached; reading deploy logs.

## Watch before the session
- Render docs — "Deploy a Node Express App"
- Traversy Media — Node deployment videos
- "CORS in 100 Seconds" — Fireship (when the frontend fails to connect)

## Task of the day
Deploy the API to Render (secrets out of the repo!), then build one page that logs in and lists + creates records against the live URL. Share it on the team board.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
