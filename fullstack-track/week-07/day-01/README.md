# Day 25 — Relationships, Filtering & Pagination

## Objective
Model connected data and make your API pleasant to consume.

## Concepts
References between collections and populate(); one-to-many patterns; query-string filtering and sorting; pagination (page & limit); text search basics.

## Watch before the session
- Net Ninja / Web Dev Simplified — Mongoose relationships & populate videos
- Traversy Media — API filtering/pagination sections of API build videos
- Mongoose docs — populate page (skim)

## Task of the day
Add a second related model (e.g. courses with enrolled students), populate it in responses, and add filtering, sorting, and pagination (5 per page) to the list endpoints.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
