# Day 23 — Authentication I — Passwords & JWT (heavy day)

## Objective
Let users register and log in securely — the feature every real product needs.

## Concepts
Why plain-text passwords end careers; hashing with bcrypt; JSON Web Tokens (what is inside one); register and login endpoints; token expiry.

## Watch before the session
- "JWT Authentication Tutorial" — Web Dev Simplified
- "Build Node.js User Authentication" — Web Dev Simplified
- "JWT in 100 Seconds" — Fireship

## Task of the day
Add a User model with hashed passwords, /register and /login endpoints returning a JWT. Decode your own token at jwt.io and write 3 sentences on what you see.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
