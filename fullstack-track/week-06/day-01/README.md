# Day 21 — Mongoose — Express Meets MongoDB (heavy day)

## Objective
Connect your API to a real database — data that survives restarts.

## Concepts
Connecting to Atlas from Express; schemas and models; validation in schemas; async/await with Mongoose; rewriting CRUD against the database.

## Watch before the session
- "Mongoose Crash Course" — Web Dev Simplified
- Net Ninja — Mongoose episodes
- Traversy Media — Node/Express/MongoDB API build videos

## Task of the day
Rebuild the students API on Mongoose: schema with validation (required fields, min/max), all CRUD against Atlas. Postman collection updated.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
