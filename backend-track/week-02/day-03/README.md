# Day 7 — Express I — Routes & Middleware

## Objective
Meet the framework — and middleware, the idea everything else in Express is built on.

## Concepts
Express setup; routing (GET/POST/PUT/DELETE); route params and query strings; middleware; express.json(); organising routes.

## Watch before the session
- "Express JS Crash Course" — Traversy Media
- "Learn Express Middleware In 14 Minutes" — Web Dev Simplified
- Net Ninja — Express episodes

## Task of the day
Build a "students API" with an in-memory array: full CRUD with correct status codes, tested in Postman with the collection saved and pushed.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
