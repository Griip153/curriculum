# Day 27 — MongoDB Aggregation & Indexes

## Objective
Answer real business questions from your data — totals, groupings, reports.

## Concepts
The aggregation pipeline ($match, $group, $sort, $project); computing totals and counts; indexes and why queries get slow without them.

## Watch before the session
- MongoDB official channel — aggregation pipeline videos
- Net Ninja / BugBytes-style aggregation walkthroughs
- MongoDB docs — aggregation quick reference

## Task of the day
Build a /stats endpoint using aggregation (e.g. total contributions per member, or stock value per category) and add an index to your most-queried field.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
