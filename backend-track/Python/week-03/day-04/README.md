# Day 12 — Testing Your API

## Objective
Prove your endpoints work automatically — before a user finds the bug for you.

## Concepts
Why tests exist; `pytest` basics; FastAPI's `TestClient` for hitting endpoints;
testing happy paths and failures; a separate test database; `pytest` as a habit.

## Watch before the session
- "Testing FastAPI Applications" — FastAPI official docs tutorial
- "Pytest Tutorial" — Tech With Tim or freeCodeCamp
- ArjanCodes — testing best practices videos

## Task of the day
Write at least 8 tests for the students API: successful CRUD plus a `422` for bad
input and a `404` for a missing record. All green. Full step-by-step instructions
are in `LESSON.md`.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
