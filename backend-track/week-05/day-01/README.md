# Day 17 — Authentication I — Passwords & JWT (heavy day)

## Objective
Let users register and log in securely — and understand exactly what a token is.

## Concepts
Hashing with bcrypt (and why plain text ends careers); JWTs — header, payload, signature; register/login endpoints; token expiry and refresh (concept).

## Watch before the session
- "JWT Authentication Tutorial" — Web Dev Simplified
- "Build Node.js User Authentication" — Web Dev Simplified
- "JWT in 100 Seconds" — Fireship

## Task of the day
Add a User model with hashed passwords and /register + /login endpoints returning a JWT. Decode your token at jwt.io and explain the three parts in your daily report.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
