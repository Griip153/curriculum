# Day 20 — Filtering, Sorting, Pagination & Search

## Objective
Make your API pleasant to consume at scale.

## Concepts
Query-string filtering; sorting; pagination (page & limit with total counts); text search; building one reusable query-features helper.

## Watch before the session
- Traversy Media — filtering/pagination sections of API build videos
- Net Ninja — relevant Mongoose episodes
- Jonas Schmedtmann-style API features videos (search "Node API filtering sorting pagination")

## Task of the day
Add filter, sort, paginate (with total pages in the response), and search to your main list endpoint — as a reusable helper, not copy-paste.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
