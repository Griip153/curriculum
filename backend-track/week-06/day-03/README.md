# Day 23 — Express + TypeScript (heavy day)

## Objective
Put it together — a typed Express application, the setup used in professional Node teams.

## Concepts
ts-node-dev setup; typing Request/Response; typed controllers and middleware; typing Mongoose models (interfaces + schemas); organising a TS backend project.

## Watch before the session
- "Express + TypeScript" setup videos — Traversy / Net Ninja
- Matt Pocock — relevant TS patterns
- A public express-typescript starter repo — read its structure

## Task of the day
Convert your Month 1 project’s core (one model, its controller, and auth middleware) to TypeScript, compiling strictly.

---
*Done? Submit your daily report in the `daily-reports` repo before midnight.*
